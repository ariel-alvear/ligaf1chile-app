<%= simple_form_for @league do |f| %>
  <% if @league.errors.any? %>
    <ul>
    <% @league.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  <% end %>
  <%= f.input :name, label: 'Nombre de la liga' %>
  <%= f.input :description, label: 'Descripción de la liga' %>
  <%= f.input :participants, label: 'Cantidad de participantes' %>
  <%= f.input :admins, label: 'Cantidad de admins' %>
  <%= f.submit 'Crear nueva liga', class: 'btn btn-success' %>
<% end %>
